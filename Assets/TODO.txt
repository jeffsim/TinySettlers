* add: primary storage: specify per-building hwt can be stored (defaults to 'anything')


* crafting station
    pause woodcutter
    let wood drop in it
    pause
    wait until worker comes to remove the wood
    while that worker is approaching, unpause

    worker should abandon task
    instead: worker continues, picks up, and hten immediately drops back down the item.
